apiVersion: v1
kind: Service
metadata:
  name: wsrestsecurity
  namespace: dev
spec:
  ports:
    - name: http
      port: 3000
      targetPort: http
    - name: https
      port: 443
      targetPort: https
  type: LoadBalancer    
  selector:
    app: wsrestsecurity
